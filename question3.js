"use strict";
//question no 3
// Name Cases: Store a person’s name in a variable, and then print that person’s name in lowercase, uppercase, and titlecase.
let faterName = "Naeem Ahmed";
console.log(faterName.toLowerCase());
console.log(faterName.toUpperCase());
let string = "maheen";
let first = string.charAt(0);
let caps = first.toUpperCase();
let rem = string.slice(1);
let combine = caps + rem;
console.log(combine);
